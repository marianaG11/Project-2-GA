<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel='stylesheet' href='/stylesheets/style2.css'/>
    <title>Document</title>
</head>
<body>
    <nav>
        <a href="/restaurants"<%- title === 'All Restaurants' ? 'class="active"' : '' %>>All Restaurants</a>
        <% if (user) { %>
          <a href="/restaurants/new" <%- title === 'Add a Restaurant' ? 'class="active"' : '' %>>Add a Restaurant</a>
          <a href="/restaurants/favorites" <%- title === 'My Favorites' ? 'class="active"' : '' %>>My Favorites</a>
          <a href="/logout">Log Out</a>
        <% } else { %>
          <a href="/auth/google">Log In With Google&nbsp;</a>
        <% } %>
    </nav>
    <div class="bg-image"></div>
    <main class="main-container">
      <div main class="main-content">
      <h1><%= title %> </h1>