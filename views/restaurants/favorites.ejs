<%- include('../partials/header') %>

<section class="restaurants-main">
    <div>
        <% favorites.forEach(function(r){ %>
        <div class="restaurants-image">
            <div class="images"><img src="<%= r.picture %>"></div>
        
        <div class="restaurant-info">
            <div>Name: <%= r.name %></div>
            <div>Food Type: <%= r.foodType %></div>
            <div>Location: <%= r.location%></div>
            <a href="/restaurants/<%=r._id%>">View More</a>
        </div>
        </div>
       <% }); %> 
    </div>
</section>




<%- include('../partials/footer') %>